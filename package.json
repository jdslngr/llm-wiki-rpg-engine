{
  "name": "archipelago-lighthouse",
  "version": "0.0.0",
  "private": true,
  "description": "A chapter-based, AI-powered text RPG set in 100,000 BCE Philippines.",
  "scripts": {
    "dev": "concurrently -k -n server,client -c blue,green \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "build": "npm --prefix client run build && npm --prefix server run build",
    "start": "npm --prefix server start"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
